services:
  pwnboard:
    build:
      context: .
      dockerfile: Dockerfile
    image: pwnboard:dev
    pull_policy: never

  worker:
    build:
      context: .
      dockerfile: Dockerfile
    image: worker:dev
    pull_policy: never